<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/widget_background"
    android:clipToOutline="true">

    <!-- Header: Ícono + Título -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/widget_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_widget_discreet"
            android:padding="6dp"
            android:background="@drawable/widget_icon_bg"
            android:contentDescription="@string/widget_icon_desc" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:text="@string/widget_default_title"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Línea 1: Texto principal -->
    <TextView
        android:id="@+id/widget_line1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/widget_default_line1"
        android:textSize="14sp"
        android:textColor="#757575"
        android:lineSpacingMultiplier="1.3"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Línea 2: Texto secundario -->
    <TextView
        android:id="@+id/widget_line2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/widget_default_line2"
        android:textSize="12sp"
        android:textColor="#9E9E9E"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Botón CTA -->
    <TextView
        android:id="@+id/widget_cta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:paddingHorizontal="14dp"
        android:paddingVertical="8dp"
        android:text="@string/widget_cta_text"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#6B4EE6"
        android:background="@drawable/widget_cta_bg" />

</LinearLayout>
