import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
import 'package:flutter/foundation.dart'
    show defaultTargetPlatform, kIsWeb, TargetPlatform;

/// Stub usado solo por CI / nuevos clones del repo.
/// Reemplaza este archivo con el real generado por `flutterfire configure`
/// para builds locales y de release. Este stub permite que `flutter analyze`
/// y `flutter test` funcionen sin exponer las credenciales reales.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    if (kIsWeb) return web;
    switch (defaultTargetPlatform) {
      case TargetPlatform.android:
        return android;
      case TargetPlatform.iOS:
        return ios;
      case TargetPlatform.macOS:
        return macos;
      case TargetPlatform.windows:
        return windows;
      default:
        throw UnsupportedError(
          'DefaultFirebaseOptions stub no soporta esta plataforma.',
        );
    }
  }

  static const FirebaseOptions web = FirebaseOptions(
    apiKey: 'AIzaSyDOCAbC123dEf456GhI789jKl012-MnO',
    appId: '1:1234567890:web:321abc456def7890',
    messagingSenderId: '1234567890',
    projectId: 'myapp-project-123',
  );

  static const FirebaseOptions android = FirebaseOptions(
    apiKey: 'AIzaSyDOCAbC123dEf456GhI789jKl012-MnO',
    appId: '1:1234567890:android:321abc456def7890',
    messagingSenderId: '1234567890',
    projectId: 'myapp-project-123',
  );

  static const FirebaseOptions ios = FirebaseOptions(
    // Public, non-production example identifiers with Firebase-valid shapes.
    // Apple Firebase SDKs reject malformed placeholders during initialization.
    apiKey: 'AIzaSyDOCAbC123dEf456GhI789jKl012-MnO',
    appId: '1:1234567890:ios:321abc456def7890',
    messagingSenderId: '1234567890',
    projectId: 'myapp-project-123',
    // Must match Runner.xcodeproj so the CI-only stub can initialize on an
    // iOS simulator. This is not a production Firebase credential.
    iosBundleId: 'com.example.appQuitar',
  );

  static const FirebaseOptions macos = ios;
  static const FirebaseOptions windows = web;
}
